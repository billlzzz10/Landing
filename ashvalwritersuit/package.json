
{
  "name": "ashval-note-assistant",
  "version": "1.0.0",
  "description": "Ashval & Note Assistant Application (Frontend-Only)",
  "main": "dist/index.html",
  "scripts": {
    "start": "echo \"Backend server removed. Run 'npm run build:frontend' and serve the 'dist' directory, or open 'dist/index.html' directly.\"",
    "build:frontend": "node build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/genai": "^1.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "fs-extra": "^11.2.0" 
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
